package com.gamecodeschool.asteroidsfs;

/*
 * Tests overall fucntionality of objectfactory.
 */
public class ObjectFactoryTester {
        // test vars
        final float zone1Min = 0.25;
        final float zone2Min = 0.50;
        // objects needed to test.
        Display testDisplay = new Display(1920, 1080);
        ObjectFactory factory = new ObjectFactory(testDisplay);

        
}
